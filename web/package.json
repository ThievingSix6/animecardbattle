{
  "name": "anime-card-legends-web",
  "version": "1.0.0",
  "private": true,
  "description": "Browser prototype of the Anime Card Legends auto-battler",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "simulate": "node scripts/simulate.mjs",
    "skills": "node scripts/skill-check.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3",
    "vite": "^8.2.2",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
